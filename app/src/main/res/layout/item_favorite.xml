<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/_10sdp">

    <!-- Location Icon -->
    <ImageView
        android:id="@+id/imageViewFavourite"
        android:layout_width="@dimen/_30sdp"
        android:layout_height="@dimen/_30sdp"
        android:src="@drawable/locationfavouritebg" />

    <!-- Address -->
    <TextView
        android:id="@+id/textViewAddress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/_10sdp"
        android:layout_toEndOf="@id/imageViewFavourite"
        android:fontFamily="@font/readexpro_regular"
        android:textColor="#000000"
        android:textSize="@dimen/_12sdp" />

    <!-- Bookmark Icon -->
    <ImageView
        android:id="@+id/imageViewFavouriteSelector"
        android:layout_width="@dimen/_25sdp"
        android:layout_height="@dimen/_25sdp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:padding="@dimen/_5sdp"
        android:src="@drawable/favouritescreenselectorbg" />

    <!-- Divider Line -->
    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="@dimen/_1sdp"
        android:layout_marginStart="@dimen/_35sdp"
        android:layout_below="@id/imageViewFavourite"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="@dimen/_10sdp"
        android:background="#E0E0E0" />
</RelativeLayout>
